import React from "react";
import "../../assets/styles/organisms/ProjectCarousel.scss";
const ProjectCarousel = ({ children }) => (
  <div className="project__carousel-container">{children}</div>
);

export default ProjectCarousel;
